---
- name: Установка bind на Ubuntu
  hosts: web_servers
  become: yes

  tasks:
    - name: Обновить кэш пакетов
      apt:
        update_cache: yes
      become: yes

    - name: Установить
      apt:
        name: bind9
        state: present
      become: yes

